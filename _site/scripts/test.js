window.onload = function() {
    if (window.jQuery) {
        // jQuery is loaded
        //alert("Test message please ignore!");
    } else {
        // jQuery is not loaded
        alert("Doesn't Work");
    }
}